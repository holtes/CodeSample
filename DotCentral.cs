using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DotCentral : MonoBehaviour
{
    public SpriteRenderer data_circle;
    public GameObject lineSmall;
    public GameObject lineBig;
    public Sprite dtCircleFinal;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void OnShowEnd()
    {
        //data_circle.sprite = dtCircleFinal;
        //lineSmall.SetActive(true);
        //lineBig.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
